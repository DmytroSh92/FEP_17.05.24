import { Box, Typography } from '@mui/material';

const Footer = () => (
    <Box component="footer" py={2} textAlign="center">
        <Typography variant="body2">© 2024 Booking App</Typography>
    </Box>
);

export default Footer;